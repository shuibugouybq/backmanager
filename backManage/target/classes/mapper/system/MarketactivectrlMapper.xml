<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.system.dao.MarketactivectrlMapper">
     
  <delete id="deleteByAllCondition"  parameterType="Marketactivectrl">
  DELETE FROM market_active_ctrl WHERE 
  MARKETACTIVEID= #{marketActiveID, jdbcType=VARCHAR}  and 
  USERCARDTYPCD= #{userCardTypCd, jdbcType=VARCHAR} and 
  CARDRANK= #{cardRank, jdbcType=VARCHAR} and 
  PAYPRODUCT= #{payProduct, jdbcType=VARCHAR} and 
  CHANNELNBR= #{channelNBR, jdbcType=VARCHAR}
  </delete>
  
  
</mapper>